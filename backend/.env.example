FLASK_APP=app.py
FLASK_DEBUG=1
# Use the local connection string for development outside Docker (if running flask run locally)
# or ensure the host matches the service name 'db' if running inside Docker.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/messenger_db
SECRET_KEY=dev-secret-key-change-in-prod